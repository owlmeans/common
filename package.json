{
  "name": "@owlmeans/common-root",
  "version": "0.1.2",
  "scripts": {
    "dev": "yarn workspaces foreach -piW -j unlimited -vv --from 'libraries/common/packages/*' --exclude 'packages/_tpl' run dev",
    "build": "yarn workspaces foreach -piW -j unlimited -vv --from 'libraries/common/packages/*' --exclude 'packages/_tpl' run build",
    "watch": "yarn workspaces foreach -piW -j unlimited -vv --from 'libraries/common/packages/*' --exclude 'packages/_tpl' run watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.5.3",
  "private": true
}
