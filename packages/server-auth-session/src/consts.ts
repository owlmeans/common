/** Seven days is an absolute session lifetime; refreshes never extend it. */
export const AUTH_SESSION_TTL = 7 * 24 * 60 * 60 * 1000

export const AUTH_SESSION_MANAGER = 'auth-session-manager'

export const AUTH_SESSION_RESOURCE = 'auth-session-record'
