/** The lib-tier i18n resource this package registers its strings under. */
export const OAUTH_I18N = 'oauth'

/** How long a consent screen's suspended sign-in stays valid — generous enough for a person to
 * pick a provider and come back, short enough that a stale suspension is not a lingering hazard. */
export const OAUTH_SUSPEND_TTL_MS = 10 * 60 * 1000
